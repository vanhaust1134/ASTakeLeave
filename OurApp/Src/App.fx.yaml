App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =Set(gblUser,LookUp('ASTakeLeave.Users_Roles',userID = LookUp('ASTakeLeave.Users','365AccountInfo'.Email = User().Email).userID));
        If(gblUser.roleID = 1,'Home Screen','Home Manage Screen')
    StartScreen: =
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

